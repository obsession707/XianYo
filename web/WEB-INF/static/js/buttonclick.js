// 登陆
// 获取模型
var modal1 = document.getElementById('id02');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal) {
        modal.style.display = "none";
    }
}// JavaScript Document

// 登陆
// 获取模型
var modal2 = document.getElementById('blogPage');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal2) {
        modal2.style.display = "none";
    }
}// JavaScript Document

// 登陆
// 获取模型
var modal2 = document.getElementById('feedbackPage');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal2) {
        modal2.style.display = "none";
    }
}// JavaScript Document

// 登陆
// 获取模型
var modal2 = document.getElementById('scenicPage');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal2) {
        modal2.style.display = "none";
    }
}// JavaScript Document

// 登陆
// 获取模型
var modal2 = document.getElementById('reportPage');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal2) {
        modal2.style.display = "none";
    }
}// JavaScript Document

// 注册
// JavaScript Document// 获取模型
var modal3 = document.getElementById('id01');

// 鼠标点击模型外区域关闭登录框
window.onclick = function (event) {
    if (event.target == modal3) {
        modal3.style.display = "none";
    }
}
